---
name: prd-rectify
description: PRD 整改
---

# Skill: prd-rectify — PRD 整改

## 触发条件

当 PRD 评审完成（`PRD_REVIEW_ISSUES.md` 已生成），需要输出整改版需求文档时使用。

## 输入

1. `docs/01-requirements/PRD_REVIEW_ISSUES.md` — 评审问题清单
2. `docs/01-requirements/PRD_RAW.md` — 原始 PRD 文档

## 输出

- `docs/01-requirements/PRD_RECTIFIED.md` — 整改后的需求文档

## 执行流程

### 步骤 1：读取输入

1. 读取 `docs/01-requirements/PRD_REVIEW_ISSUES.md`，提取所有 🔴 阻塞和 🟡 重要问题
2. 读取 `docs/01-requirements/PRD_RAW.md`

### 步骤 2：逐项整改

针对每个阻塞和重要问题：

1. 找到 PRD 中对应的内容
2. 按评审建议进行修改
3. 使用标记标注变更：【新增】、【修改】、【待确认】

### 步骤 3：补全缺失内容

检查并补全以下内容：

| 检查项 | 要求 |
|---|---|
| 功能编号 | 每个功能点必须有唯一编号（F001、F002…） |
| 角色定义 | 明确系统涉及的所有角色及权限 |
| 业务流程 | 核心流程用文字或伪流程图描述 |
| 字段清单 | 每个表单/页面的字段、类型、必填、校验规则 |
| 业务规则 | 所有业务判断逻辑必须量化和明确 |
| 异常场景 | 每个功能点的异常流和错误提示 |
| 边界条件 | 数值范围、字符长度、数量上限等 |
| 模块展示 | 所属模块必须使用 `EN（中文）`（如 `USER（用户管理）`） |
| 非功能需求 | 性能指标、安全要求、兼容性要求 |

### 步骤 4：输出整改文档

按以下结构输出 `docs/01-requirements/PRD_RECTIFIED.md`（按章节标题对齐模板契约）：

```markdown
# 整改后需求文档（PRD）

## 文档信息
- 文档类型：产物
- 生成 Skill：`prd-rectify`
- 上游输入：`PRD_RAW.md`、`PRD_REVIEW_ISSUES.md`
- 版本：v1.0
- 日期：YYYY-MM-DD
- 状态：已整改 / 已冻结

## 1. 基线概述
### 1.1 项目背景
### 1.2 本期目标
### 1.3 成功标准
### 1.4 范围界定

## 2. 角色与权限
| 角色 | 描述 | 核心权限 | 关联功能 |
|---|---|---|---|

## 3. 功能需求基线
### F001: [功能名称]
- **所属模块**：USER（用户管理）
- **描述**：
- **角色**：
- **前置条件**：
- **主流程**：
- **字段清单**：
  | 字段名 | 类型 | 必填 | 校验规则 | 说明 |
  |---|---|---|---|---|
- **业务规则**：
- **异常场景**：
- **边界条件**：
- **验收标准**：

## 4. 全局规则与非功能需求
### 4.1 通用业务规则
### 4.2 性能要求
### 4.3 安全要求
### 4.4 审计与日志要求
### 4.5 兼容性要求

## 5. 原型/页面映射
| 页面/原型 | 目标 | 关联功能 | 关键元素 | 备注 |
|---|---|---|---|---|

## 6. 追溯矩阵
| 需求编号 | 来源材料/澄清 | 计划接口 | 计划数据表 | 计划测试 |
|---|---|---|---|---|

## 7. 遗留待确认与风险
| 编号 | 项目 | 风险说明 | 对下游影响 | 处理策略 |
|---|---|---|---|---|

## 8. 术语表
| 术语 | 定义 |

## 变更记录
| 版本 | 日期 | 说明 |
|---|---|---|
```

### 步骤 5：交叉验证

整改完成后，对照评审问题清单逐条验证：

- [ ] 所有 🔴 阻塞问题已解决
- [ ] 所有 🟡 重要问题已解决或标记【待确认】
- [ ] 功能编号完整且连续
- [ ] 模块展示字段符合 `EN（中文）`
- [ ] 每个功能点都有异常场景
- [ ] 字段清单完整

### 步骤 6：执行需求基线冻结

在通过步骤 5 后执行冻结确认：

1. 若仍有未关闭的 🔴 阻塞问题，将文档状态置为`已整改`并中止下游阶段
2. 若阻塞问题全部关闭，将文档状态更新为`已冻结`
3. 在变更记录中增加冻结记录（示例：`v1.0 / YYYY-MM-DD / 完成评审整改并冻结需求基线`）

### 步骤 7：提示下一步

整改完成后提示用户：
- 若 `PRD_RECTIFIED.md` 已冻结，下一步使用 `solution-design` 生成架构、接口、数据模型设计
- 若仍为`已整改`，先回到评审问题清单继续关闭阻塞项
- 注意：`qa-design` 依赖设计文档作为输入，必须先完成 `solution-design`

## 注意事项

- 整改必须基于评审问题，不要无中生有增加需求
- 无法确定的内容标记【待确认】，不要自行决定
- 整改后的文档是后续所有设计和开发的唯一需求来源
